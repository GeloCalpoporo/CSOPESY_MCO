# Both processes exist and are running - the second was not turned away.
+ p1
+ p2
# Memory is completely committed.
+ 512 bytes used memory
+ 0 bytes free memory
# ... and the two of them are swapping heavily through the backing store.
~ [1-9][0-9]* num paged in
~ [1-9][0-9]* num paged out
+ CSOPESY BACKING STORE
- (no pages currently in the backing store)
# A quantum of 0 under FCFS must not stall or crash anything.
- invalid
# --- the machine must actually RETIRE INSTRUCTIONS, not just page ---
# This case originally livelocked: acquiring one page at a time let each process take a
# page, fail to get the second, drop it, and lose it to the other before its restart.
# vmstat looked busy (tens of thousands of page-ins) while screen -ls sat frozen at ~50
# instructions and under 200 active ticks. Everything above still passed. These three
# lines are what would have caught it, so they stay.
~ [0-9]{4,} active cpu ticks
~ ^p1 +\(.*[0-9]{3,} / 4000
~ ^p2 +\(.*[0-9]{3,} / 4000
