# The scheduler named in config.txt is the one that was used.
+ fcfs on 2 core(s)
+ Running Processes:
+ Finished Processes:
~ Finished +[0-9]+ / [0-9]+
# Only 2 cores exist, so a core id of 2 or higher would be a dispatch bug.
~ Core [01]:
- Core 2:
- Core 3:
# The CPU was busy, so the tick accounting must show it.
~ [1-9][0-9]* active cpu ticks
