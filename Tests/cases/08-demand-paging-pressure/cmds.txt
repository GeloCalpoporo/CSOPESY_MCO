# MCO2 spec sections 2, 3 and 8: demand paging, page replacement, and a
# backing store you can open and read. Only 16 frames exist, so many more
# processes than that must force real eviction and real page-ins.
initialize
scheduler-start
@sleep 6
scheduler-stop
vmstat
process-smi
exit
