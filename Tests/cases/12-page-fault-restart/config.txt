# 256 bytes of RAM in 4 frames of 64. The process below asks for 2048 bytes,
# i.e. 32 pages, so at most 4 of its 32 pages can ever be resident at once.
# It CANNOT run without demand paging working correctly.
num-cpu 1
scheduler "fcfs"
quantum-cycles 5
batch-process-freq 1
min-ins 100
max-ins 200
delay-per-exec 0
max-overall-mem 256
mem-per-frame 64
min-mem-per-proc 64
max-mem-per-proc 128
